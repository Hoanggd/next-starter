export default function GettingStartedPage() {
  return (
    <div>
      We use the Shadcn UI library for most of our components since it provides
      accessible, customizable building blocks for our UI. However, Shadcn
      doesn't support everything we need, so we added a few extra components to
      cover those gaps.
    </div>
  );
}
